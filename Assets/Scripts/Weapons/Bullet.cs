using UnityEngine;

public class Bullet : MonoBehaviour
{
    [SerializeField]
    private string _resourceName = "Nerf Bullet";

    public string ResourceName => _resourceName;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    public void Reserve()
    {

    }
}
